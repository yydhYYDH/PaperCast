% ---------------------------------------------------------------------------
% 本文件是 beamerthemesustech-elements.sty 的去品牌化 fork（PaperCast 论文分享流水线使用）。
% 上游：github.com/yhbcode000/sustech-slides-template，Apache-2.0，
%       Copyright (c) 2026 杨昊波 (Haobo Yang)。许可证全文见同目录 LICENSE-upstream。
% 本 fork 的改动：包名/宏名/颜色名 sustech -> papercast；默认不显示校徽与出处行。
% ---------------------------------------------------------------------------
% beamerthemepapercast-elements.sty
% Theme author: 杨昊波 (Haobo Yang) — 2026 届计算机系博士 · github.com/yhbcode000 · yhbcode000@foxmail.com
% 可复用排版宏与环境（基于标准 beamer 构件，键入 PaperCast 学术配色）。
% 由 beamerthemepapercast.sty 在加载 color 主题之后引入，故可直接使用
% papercastorange / papercastblue / papercastgold / papercastink 等颜色。
\ProvidesPackage{beamerthemepapercast-elements}[2026/06/25 PaperCast themed elements (Madrid-based)]

\RequirePackage{graphicx}
\RequirePackage{booktabs}
\RequirePackage{colortbl}
\RequirePackage{amsmath}

% --- 文字配色别名（强调 / 关键词 / 表头）已移至 beamercolorthemepapercast.sty：
% 样式层只引用语义角色名（emph / kw / hdr），具体色值由配色方案决定。
% 新增配色方案时无需改动本文件。

% --- 强调 / 高亮 -------------------------------------------------------------
\newcommand{\shl}[1]{\textbf{\textcolor{emph}{#1}}}        % 强调（橙、粗）
\newcommand{\keyword}[1]{\textbf{\textcolor{kw}{#1}}}      % 关键词（橙、粗）
\newcommand{\brandemph}[1]{\textit{\textcolor{emph}{#1}}}  % 品牌强调（橙、斜）
\newcommand{\hlbox}[1]{\colorbox{papercastgold}{#1}}       % 强调色底高亮（重要数字，实色）

% --- 图与图注 ----------------------------------------------------------------
% \fitfigure{file}：限宽限高、保持比例，避免溢出
\newcommand{\fitfigure}[1]{\includegraphics[width=\linewidth,height=0.45\textheight,keepaspectratio]{#1}}
% 图注前缀（默认“图”，可用 \setfigureprefix 改为 Figure 等）
\newcommand{\figureprefix}{图}
\newcommand{\setfigureprefix}[1]{\renewcommand{\figureprefix}{#1}}
% \figcap{n}{文字} -> "图 n: 文字" — 自动换行并居中，标题始终在图下方
\newcommand{\figcap}[2]{\par\smallskip{\centering\footnotesize\color{papercastgrey}\mbox{\figureprefix~#1:}~#2\par}}

% --- 指标大数字 --------------------------------------------------------------
% \metric{100\%}{成功率}
\newcommand{\metric}[2]{%
  \begin{minipage}[t]{0.30\textwidth}\centering
    {\fontsize{22}{24}\selectfont\textbf{\textcolor{emph}{#1}}}\\[0.2em]
    {\small\color{papercastgrey}#2}
  \end{minipage}}

% --- 要点框 callout（橙色 alertblock）----------------------------------------
\newenvironment{callout}[1][要点]{\begin{alertblock}{#1}}{\end{alertblock}}

% --- 表格辅助 ----------------------------------------------------------------
\newcommand{\theadrow}{\rowcolor{hdr}}     % 表头底色行（主蓝）
\newcommand{\altrow}{\rowcolor{cardgrey}}  % 隔行浅灰
\newcommand{\thc}[1]{{\color{thc}\bfseries #1}}

% --- 间距节律 ----------------------------------------------------------------
\newcommand{\tightgap}{\vspace{0.18em}}
\newcommand{\deckgap}{\vspace{0.45em}}

% --- 分区宽度 ----------------------------------------------------------------
\newcommand{\zonetext}{0.55\textwidth}
\newcommand{\zonefig}{0.42\textwidth}
\newcommand{\zonehalf}{0.48\textwidth}

% --- 双栏分屏（两个独立"迷你幻灯片"共享一页）-------------------------------
% \twopane{<left content>}{<right content>}
% Left ~33%, right ~63%, thin grey rule between them. 1:2 ratio gives more
% room for insight blocks on the right while keeping the index readable.
% Use inside \begin{frame}{title} ... \end{frame}.
\newcommand{\twopane}[2]{%
  \begin{minipage}[t][0.80\textheight][t]{0.31\textwidth}\vspace{0pt}%
    #1%
  \end{minipage}%
  \hfill{\color{papercastgrey}\vrule width 0.5pt}\hfill%
  \begin{minipage}[t][0.80\textheight][t]{0.62\textwidth}\vspace{0pt}%
    #2%
  \end{minipage}%
}

% --- 紧凑 TOC（通过 sty 调整 \tableofcontents 间距）--------------------------
\newcommand{\tocdense}{%
  \addtobeamertemplate{section in toc}{\vskip-1.2em\hskip2em}{}%
  \setlength{\parskip}{-0.8em}%
}

% --- 章节分隔页（Part 编号 + 标题 + 一句话主旨）------------------------------
% 在每个 \section 之前用 \renewcommand{\secblurb}{...} 设置本节主旨。
\newcommand{\secblurb}{}
% 可视化分析节（\vizbgon 触发）标题页文字放入白色面板，保证在拼贴背景上可读；
% 其余分节页保持原始样式。条件由主题自带声明（默认关闭），文档中用
% \vizbgon / \vizbgoff 切换；切换必须在 frame 环境之外（帧体收集器限制）。
% \newif 兼容旧文档中残留的 \newif\ifvizbgdiv 声明（重复声明无害）。
\newif\ifvizbgdiv
\vizbgdivfalse
\newcommand{\vizbgon}{\vizbgdivtrue}
\newcommand{\vizbgoff}{\vizbgdivfalse}
\setbeamercolor{vizdivbox}{bg=white!85!black}
\AtBeginSection[]{%
  \ifvizbgdiv
    \begin{frame}[plain]
      \vfill
      \vspace*{0.045\textheight}% 校正 [plain] 帧体偏上偏移，保证面板严格垂直居中
      \noindent\hbox to \textwidth{\hss
      \begin{beamercolorbox}[wd=0.82\textwidth,center,sep=14pt,rounded=true]{vizdivbox}
        \vspace*{2.1em}% 平衡 Part（上方）与 rule+blurb（下方）的高度差，让标题在面板内垂直居中
        {\color{papercastgrey}\large Part~\thesection}\\[0.5em]
        {\color{primtext}\Huge\bfseries\insertsectionhead}\\[0.7em]
        {\color{emph}\rule{2cm}{2pt}}\\[0.8em]
        {\color{papercastink}\normalsize\secblurb}
      \end{beamercolorbox}\hss}
      \vfill
    \end{frame}
  \else
    \begin{frame}[plain]
      \vfill
      \vspace*{0.045\textheight}% 校正 [plain] 帧体偏上偏移，保证标题严格垂直居中
      \noindent\hbox to \textwidth{\hss
      \parbox{0.82\textwidth}{\centering
        {\color{papercastgrey}\large Part~\thesection}\\[0.6em]
        {\color{primtext}\Huge\bfseries\insertsectionhead}\\[0.8em]
        {\color{emph}\rule{2cm}{2pt}}\\[1.0em]
        {\color{papercastink}\normalsize\secblurb}
      }\hss}
      \vfill
    \end{frame}
  \fi}

\mode<all>
